import Link from "next/link";
import { BrandLogo } from "@/components/BrandLogo";
export default function Home() { return <main className="mx-auto max-w-7xl p-6 lg:p-8"><section className="panel overflow-hidden px-8 py-10 lg:px-12 lg:py-14"><div className="grid gap-10 lg:grid-cols-[minmax(0,1.2fr)_minmax(320px,0.8fr)] lg:items-center"><div><BrandLogo textClassName="text-3xl" /><p className="eyebrow mt-10">Kubernetes observability</p><h1 className="mt-4 max-w-3xl text-5xl font-black tracking-tight text-[var(--text)] lg:text-6xl">Operate private clusters with clear incident context and guided next steps.</h1><p className="mt-5 max-w-2xl text-lg leading-8 text-[var(--text-muted)]">ClusterSage keeps onboarding, cluster health, resource inspection, incident review, and AI-backed investigation in one focused console without requiring direct access to your cloud account.</p><div className="mt-8 flex flex-col gap-3 sm:flex-row"><Link href="/register" className="btn">Create workspace</Link><Link href="/login" className="btn-secondary">Log in</Link></div></div><div className="panel-subtle p-6 lg:p-8"><p className="eyebrow">What the workspace covers</p><div className="mt-5 grid gap-4"><div className="panel bg-transparent p-4 shadow-none"><h2 className="font-semibold text-[var(--text)]">Connected clusters</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Track agent status, last-seen telemetry, and inventory across clusters owned by your organization.</p></div><div className="panel bg-transparent p-4 shadow-none"><h2 className="font-semibold text-[var(--text)]">Resource investigation</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Move from workloads to logs, incidents, and suggestions without losing cluster context.</p></div><div className="panel bg-transparent p-4 shadow-none"><h2 className="font-semibold text-[var(--text)]">Controlled remediation</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Review AI suggestions and approvals with explicit guardrails before any agent action can proceed.</p></div></div></div></div></section><section className="mt-8 grid gap-4 md:grid-cols-3"><div className="card"><p className="eyebrow">Agent model</p><h2 className="mt-3 text-xl font-semibold">Built for private clusters</h2><p className="mt-3 text-sm leading-6 text-[var(--text-muted)]">The collector runs inside the cluster and sends telemetry outward, so operators can keep restrictive network boundaries in place.</p></div><div className="card"><p className="eyebrow">Investigation flow</p><h2 className="mt-3 text-xl font-semibold">Resource-first troubleshooting</h2><p className="mt-3 text-sm leading-6 text-[var(--text-muted)]">Start from inventory, then drill into incidents, logs, alert limits, and clustered AI summaries using the same tenant-scoped data.</p></div><div className="card"><p className="eyebrow">Operational clarity</p><h2 className="mt-3 text-xl font-semibold">Incident context that stays grounded</h2><p className="mt-3 text-sm leading-6 text-[var(--text-muted)]">ClusterSage surfaces what the backend actually stores and correlates, instead of hiding important investigation details behind generic dashboards.</p></div></section></main>; }
