import Link from "next/link";
import { AuthForm } from "@/components/AuthForm";
export default function LoginPage() { return <main className="mx-auto grid min-h-screen max-w-7xl gap-10 p-8 lg:grid-cols-[minmax(0,1.05fr)_minmax(420px,0.95fr)] lg:items-center"><section className="space-y-6"><p className="eyebrow">ClusterSage access</p><h1 className="max-w-2xl text-5xl font-black tracking-tight text-[var(--text)]">Investigate cluster health with a workspace that stays grounded in real telemetry.</h1><p className="max-w-2xl text-lg leading-8 text-[var(--text-muted)]">Review connected clusters, alert thresholds, incident evidence, and controlled remediation workflows from the same console your team uses every day.</p><div className="grid gap-4 sm:grid-cols-2"><div className="panel p-5"><h2 className="text-lg font-semibold">Organization-scoped access</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">All clusters, incidents, and AI guidance remain isolated to the signed-in organization.</p></div><div className="panel p-5"><h2 className="text-lg font-semibold">Cluster operations workflow</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Move from onboarding to detailed cluster investigation without changing products or dashboards.</p></div></div></section><section><AuthForm mode="login" /><p className="mt-4 text-center text-sm text-[var(--text-muted)]">Need an account? <Link className="font-medium text-[var(--primary)]" href="/register">Create a workspace</Link></p></section></main>; }
