import Link from "next/link";
import { AuthForm } from "@/components/AuthForm";
export default function LoginPage() { return <main className="mx-auto grid min-h-screen max-w-7xl gap-10 p-8 lg:grid-cols-[minmax(0,1.05fr)_minmax(420px,0.95fr)] lg:items-center"><section className="space-y-6"><p className="eyebrow">ClusterSage access</p><h1 className="max-w-2xl text-5xl font-black tracking-tight text-[var(--text)]">Log in and pick up where your team left off.</h1><p className="max-w-2xl text-lg leading-8 text-[var(--text-muted)]">Open clusters, review incidents, and continue active investigations from one workspace.</p><div className="grid gap-4 sm:grid-cols-2"><div className="panel p-5"><h2 className="text-lg font-semibold">Shared workspace</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Clusters, incidents, and alerts stay together in one place.</p></div><div className="panel p-5"><h2 className="text-lg font-semibold">Focused workflow</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Move from overview to cluster detail without extra setup screens.</p></div></div></section><section><AuthForm mode="login" /><p className="mt-4 text-center text-sm text-[var(--text-muted)]">Need an account? <Link className="font-medium text-[var(--primary)]" href="/register">Create a workspace</Link></p></section></main>; }
