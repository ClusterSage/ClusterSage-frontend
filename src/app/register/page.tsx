import Link from "next/link";
import { AuthForm } from "@/components/AuthForm";
export default function RegisterPage() { return <main className="mx-auto grid min-h-screen max-w-7xl gap-10 p-8 lg:grid-cols-[minmax(0,1.05fr)_minmax(420px,0.95fr)] lg:items-center"><section className="space-y-6"><p className="eyebrow">Set up ClusterSage</p><h1 className="max-w-2xl text-5xl font-black tracking-tight text-[var(--text)]">Create a workspace for your team.</h1><p className="max-w-2xl text-lg leading-8 text-[var(--text-muted)]">Once you’re in, you can connect clusters and start reviewing activity in one place.</p><div className="grid gap-4 sm:grid-cols-2"><div className="panel p-5"><h2 className="text-lg font-semibold">Connect clusters</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Set up access and register your first cluster.</p></div><div className="panel p-5"><h2 className="text-lg font-semibold">Review issues</h2><p className="mt-2 text-sm leading-6 text-[var(--text-muted)]">Track incidents, limits, and suggestions from the same workspace.</p></div></div></section><section><AuthForm mode="register" /><p className="mt-4 text-center text-sm text-[var(--text-muted)]">Already registered? <Link className="font-medium text-[var(--primary)]" href="/login">Log in</Link></p></section></main>; }
